zerops:
  - setup: nodejs
    build:
      base: nodejs@20
      buildCommands:
        - npm i
      deployFiles:
        - ./
      cache:
        - node_modules
    run:
      start: node index.js
      ports:
        - port: 3000
          http:
            routes:
              - port: 3000
